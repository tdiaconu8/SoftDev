# Write a command to count the words longer than 5 characters in bb.txt
grep -o '[A-Za-z][A-Za-z]*[A-Za-z]' | awk '{if(length($0)>5) print $0}'

# Write the mean of tests for each candidate
awk 'BEGIN {FS=","; OFS=" "} {if(NR!=1) print($1, ($3 + $4 + $5)/3)}'


